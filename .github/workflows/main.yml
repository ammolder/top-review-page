name: Publish Docker
on: 
  push:
    branches: 
      - main
jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - name: Publish to registry
        run: echo AndrewMiller21 | docker login -u ammolder --password-stdin

